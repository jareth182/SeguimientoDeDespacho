@{
    ViewData["Title"] = "Listado de Despachos (HU09)";
    Layout = "_AdminLayout"; // <-- ¡AÑADE ESTA LÍNEA AL INICIO!
}

<h1>@ViewData["Title"]</h1>
<p>Esta es la página placeholder para la HU09 (Listado de Despachos).</p>

<hr />

@if(ViewData["FiltroAplicado"] != null)
{
    <div class="alert alert-info">
        <strong>Criterio 3 (HU03) CUMPLIDO:</strong>
        El filtro se aplicó correctamente.
        <br>
        Estado solicitado: <strong>@ViewData["FiltroAplicado"]</strong>
    </div>
}